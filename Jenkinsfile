pipeline {
    agent any

    tools {
        
        maven "maven"
    }

    stages {
        stage('Build') {
            steps {
                      
                      "mvn clean install"


            }

            
        }
    }
}
